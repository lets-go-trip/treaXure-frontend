// vue.config.js
module.exports = {
  // Disable ESLint for development
  lintOnSave: false,
  
  // Optional: Configure webpack Dev Server
  devServer: {
    // Show progress
    progress: true,
    // Open browser automatically
    open: true
  }
} 